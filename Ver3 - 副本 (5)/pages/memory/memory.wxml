<!-- 状态栏占位 -->
<view class="status-bar" style="height: {{statusBarHeight}}px;"></view>

<!-- 自定义导航栏 -->
<view class="custom-nav">
  <view class="nav-left" bindtap="goBack">
    <text class="back-icon">‹</text>
  </view>
  <view class="nav-title">记忆面板</view>
  <view class="nav-right">
    <text class="more-icon">⋯</text>
  </view>
</view>

<scroll-view class="memory-container" scroll-y enhanced show-scrollbar="{{false}}">
  
  <!-- 记忆摘要 -->
  <view class="memory-summary">
    <view class="summary-header">
      <image class="summary-avatar" src="/images/icon-avatar.png"></image>
      <view class="summary-info">
        <text class="summary-title">AI 记住的你</text>
        <text class="summary-count">共 {{totalCount}} 条记忆</text>
        <view class="summary-stats">
          <text class="stat-item">画像 {{profileCount}}</text>
          <text class="stat-item">目标 {{goalCount}}</text>
          <text class="stat-item" wx:if="{{contextCount > 0}}">可续接会话 {{contextCount}}</text>
        </view>
      </view>
    </view>
    <text class="summary-desc">长期信息按类型保存；会话上下文单独续接，不会混入你的长期画像</text>
  </view>

  <!-- 记忆列表 -->
  <view class="memory-list">
    <view 
      class="memory-item"
      wx:for="{{memories}}"
      wx:key="key"
      data-key="{{item.key}}"
      bindtap="editMemory"
      bindlongpress="deleteMemory"
    >
      <view class="memory-header">
        <view class="memory-tag" style="background: {{item.color}}20; color: {{item.color}}">
          {{item.category}}
        </view>
        <text class="memory-time">{{item.updated_at}}</text>
      </view>
      <text class="memory-key">{{item.label}}</text>
      <text class="memory-value">{{item.displayValue}}</text>
      <view class="memory-edit-hint">
        <text class="edit-icon">✎</text>
        <text>点击编辑，长按删除</text>
      </view>
    </view>
  </view>

  <!-- 底部留白 -->
  <view style="height: 48rpx;"></view>
</scroll-view>
