# 企业战略管理 21 页资料验收

测试日期：2026-09-04。只测试资料上传、识别与来源单位，不生成 Card，不修改前端或生产数据库。

## 原文件

- 文件：企业战略管理分章重点【21页】.pdf
- 大小：1056437 字节，约 1.01 MiB。
- 页数：21；全部有文字层，全部 0 个内嵌图片对象。
- SHA-256：`dca3544fa8d5e5d163d1e7e53073e5ad3905f795adf05b7f5b9bcf8928e7e6f0`。
- 未复制原始 PDF 到仓库，未修改原件。

## 自动测试结果

- `machine_pages.jsonl`：全 21 页的原始文字、safe_text、来源方式和机器状态。
- `machine_summary.json`：21 页解析成功，0 页解析错误；20777 字符（包含空格和换行）。
- `manifest.json`：文件指纹、页码、依赖版本、配置和相关源代码指纹。
- `api_summary.json`：注册、登录、上传、哈希校验、解析、Unit 查询、页序、重复解析幂等、未登录 401、跨账号 404 均通过。
- `api_smoke.py`：复现实测的隔离脚本，使用内存 SQLite 和自动清理的临时上传目录，不运行真实数据库启动钩子。
- API 测试的上传耗时 0.0267 秒，完整解析耗时 0.0847 秒；这是本机进程内 TestClient 计时，不是公网/并发性能测试。
- 生成 21 个 Unit，全部 `extraction_method=native`；OCR 和 LLM 调用数均为 0，Card 创建数为 0。

## 质量边界与视觉抽查

**21 个 accepted 是当前原生文字分支的默认标记，不代表经过了 OCR 段落质量门，也不是 100% 人工准确率。**

助手对第 1、10、11、18、19、21 页进行了原图视觉抽查，并查看了全 21 页首尾文字：

- 抽查区域的中文、编号和主要阅读顺序与页面显示相符，未发现明显乱码。
- 第 1 页的“主要观点”在第 2 页继续列举。这是跨页的结构关系，当前两个 Unit 没有连接。
- 第 10 页末尾“整合营销”的定义在第 11 页继续，当前分别保存于两个 Unit。文字未丢失，但不能把任意一页片段视为独立完整定义。
- 第 18 页末尾“长期负债与权益”与第 19 页开头“资本之间的比例关系要合理）”构成跨页内容。
- 第 21 页仅有两行、37 字符，是第 20 页内容的延续，并非识别失败或广告页。
- Poppler 渲染产生 `Unknown character collection 'Adobe-Identity-H'` 警告，仍生成了可读页面图像；抽查据完整页面进行。

本轮没有对全部 20777 字符进行独立人工标注，也未核验资料原文的学科知识正确性。

## 结论

这份文字型 PDF 的上传和文本提取链路可用，比图片型土力学资料更适合作为第一批 MVP 输入。
尚存的主要问题是按页保存导致的跨页段落/列表关系割裂，而非 OCR 字符识别。
当前 OCR 段落过滤修复不会自动作用于 native 分支。进入后续学习内容生成前，需要单独处理跨页完整性；本轮仅记录，未修改产品逻辑。
