<bottom-sheet visible="{{visible}}" title="加入行动" submit-label="确认加入" submitting="{{submitting}}" bind:close="close" bind:submit="submit"><view class="sync"><text class="sync__title">{{phase.label}} · {{phase.title}}</text><text class="sync__copy">本阶段的 {{phase.tasks.length || 0}} 项任务会成为可完成的待办。已有截止日期会保留，其余任务按阶段周期安排。</text><text class="label">阶段开始日期</text><picker mode="date" value="{{startDate}}" bindchange="change"><view class="field">{{startDate}}</view></picker><text class="note">重复同步不会创建第二份任务。</text></view></bottom-sheet>
