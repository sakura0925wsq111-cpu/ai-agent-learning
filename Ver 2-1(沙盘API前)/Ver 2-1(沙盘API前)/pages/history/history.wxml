<!-- 状态栏占位 -->
<view class="status-bar" style="height: {{statusBarHeight}}px;"></view>

<!-- 自定义导航栏 -->
<view class="custom-nav">
  <view class="nav-left" bindtap="goBack">
    <text class="back-icon">‹</text>
  </view>
  <view class="nav-title">历史记录</view>
  <view class="nav-right">
    <view class="filter-btn" bindtap="showFilter">
      <text>{{currentFilter}}</text>
      <text class="filter-arrow">▼</text>
    </view>
  </view>
</view>

<!-- 标签切换 -->
<view class="tab-bar">
  <view 
    class="tab-item {{currentTab === 'plan' ? 'active' : ''}}"
    data-tab="plan"
    bindtap="switchTab"
  >
    <text>规划历史</text>
    <view class="tab-line" wx:if="{{currentTab === 'plan'}}"></view>
  </view>
  <view 
    class="tab-item {{currentTab === 'chat' ? 'active' : ''}}"
    data-tab="chat"
    bindtap="switchTab"
  >
    <text>对话历史</text>
    <view class="tab-line" wx:if="{{currentTab === 'chat'}}"></view>
  </view>
</view>

<scroll-view class="history-container" scroll-y enhanced show-scrollbar="{{false}}">
  
  <!-- 规划历史 -->
  <view class="history-list" wx:if="{{currentTab === 'plan'}}">
    <view 
      class="history-card"
      wx:for="{{planHistory}}"
      wx:key="id"
      data-id="{{item.session_id}}"
      data-type="{{item.agent_type}}"
      bindtap="viewPlanDetail"
    >
      <view class="history-header">
        <view class="history-type-tag" style="background: {{item.color}}20; color: {{item.color}}">
          {{item.typeName}}
        </view>
        <text class="history-status {{item.status}}">{{item.statusText}}</text>
      </view>
      <text class="history-title">{{item.title}}</text>
      <text class="history-desc">{{item.summary}}</text>
      <view class="history-footer">
        <text class="history-date">{{item.created_at}}</text>
        <view class="history-arrow">→</view>
      </view>
    </view>
  </view>

  <!-- 对话历史 -->
  <view class="history-list" wx:if="{{currentTab === 'chat'}}">
    <view 
      class="history-card chat-card"
      wx:for="{{chatHistory}}"
      wx:key="id"
      data-id="{{item.session_id}}"
      bindtap="viewChatDetail"
    >
      <view class="chat-header">
        <image class="chat-avatar" src="/images/ai-avatar.png"></image>
        <view class="chat-info">
          <text class="chat-title">{{item.title}}</text>
          <text class="chat-date">{{item.last_message_time}}</text>
        </view>
        <view class="chat-count">{{item.message_count}}条消息</view>
      </view>
      <text class="chat-preview">{{item.last_message}}</text>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty-state" wx:if="{{currentTab === 'plan' && planHistory.length === 0}}">
    <image class="empty-icon" src="/images/empty-history.png"></image>
    <text class="empty-title">暂无规划记录</text>
    <text class="empty-desc">完成一次规划后，记录会出现在这里</text>
  </view>

  <view class="empty-state" wx:if="{{currentTab === 'chat' && chatHistory.length === 0}}">
    <image class="empty-icon" src="/images/empty-chat.png"></image>
    <text class="empty-title">暂无对话记录</text>
    <text class="empty-desc">和 AI 聊聊后，对话会保存在这里</text>
  </view>

  <!-- 底部留白 -->
  <view style="height: 48rpx;"></view>
</scroll-view>
