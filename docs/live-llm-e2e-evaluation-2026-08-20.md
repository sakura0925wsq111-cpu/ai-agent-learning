# 真实 LLM 全链路评估记录（2026-08-20）

## 范围与隔离方式

- 模型：`deepseek-chat`。
- 用户、业务数据库和 LangGraph checkpoint 均使用临时隔离环境；测试结束后已关闭并清理。
- 本记录不包含 API Key、Cookie、Token 或真实用户数据。

## 验收链路

```text
选择 2 条成长路径
→ 完成沙盘问答
→ 生成路径对比
→ 选择就业方向
→ 生成四阶段规划
→ 同步第一阶段
→ 完成一项任务
→ 进度变化
→ AI 教练读取最新执行情况
```

## 真实沙盘聊天摘录

**AI（首条，模型生成）**

> 已记录你选择的就业规划、考研规划。
>
> 第一轮：你目前是否已经开始了与就业或考研相关的任何具体准备，比如投过简历、联系过导师，或者已经报名了某个课程？

**用户**

> 我在计算机专业大三，做过校园二手交易小程序，负责后端接口和部署，还没有正式实习。

**AI 行为检查**

- 未要求再次选择路径。
- 未问“更倾向就业还是读研”。
- 未出现“你知道/你了解薪资或政策吗”式知识测验问题。
- 后续围绕项目经历、地域与时间约束、数学英语基础和就业经历继续收集事实。

**后续用户补充**

> 希望毕业后在杭州或上海发展；家庭可支持继续学习，但不希望长期没有收入。
>
> 数学和英语基础一般，尚未系统备考，担心错过求职窗口。
>
> 有后端项目，但没有实习和正式投递经历。
>
> 考研尚未开始，数学和英语需要从基础补起。

## 沙盘结果

- 已选路径：`career`、`graduate`。
- 锁定状态：`true`，对话中未被模型改写。
- 最终阶段：`completed`。
- 已生成两路径对比报告与路径投影。
- 测试选择的后续方向：`career`（就业规划）。

## 成长规划与同步结果

| 验收项 | 实测结果 |
| --- | --- |
| 四阶段规划 | 成功生成 `phase_1` 至 `phase_4` 共 4 个阶段 |
| 第一阶段同步 | 成功同步 3 个待办 |
| 今日总览 | 同步后读取到 3 个待办 |
| 完成任务 | 将第一项待办标记为 `done` 成功 |
| 执行进度 | `1 / 3`，整体完成度 `33%` |
| AI 教练 | 能读取报告、同步待办和最新完成状态后给出下一步建议 |

第一阶段实际同步的任务包含：完成 Java REST API 学生管理功能、使用 Python 抓取并分析数据、整理 Java/Python 技术学习路线并输出 Markdown 笔记。任务按第一阶段开始日期分配了截止日期。

## 结论

该链路在真实线上 LLM、隔离数据库和真实服务调用下已跑通。关键边界包括：沙盘完成状态、路径对比、方向交接、四阶段报告持久化、待办同步、完成状态回写、进度聚合和教练上下文读取，均得到实际结果验证。
